<div class="container-fluid main-bg pt-4">
  <div class="text-center">
    <h3><%= @batch.city %>#<%= @batch.number %></h3>
  </div>
  <%= render partial: "batch_products/product_row", collection: @products, as: :batch_product %>

  <div>
    <p>
      <%= link_to "Add Row", new_batch_product_path, remote: true, data: { type: 'html' } %>
    </p>
  </div>
</div>
